Text field with optional label, leading icon, hint and error state. Used on the login screen and search.

```jsx
<Input label="E-Mail" iconLeft="mail" type="email" placeholder="du@beispiel.de" />
<Input label="Passwort" iconLeft="lock" type="password" error="Falsches Passwort" />
```
