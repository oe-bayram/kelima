The four self-assessment / examiner ratings shown after each card. Maps 1:1 to status colors and the repetition intervals (Nicht gewusst→gleich, Schwer→morgen, Kann ich→3 Tage, Sicher→7 Tage).

```jsx
<RatingButtons onRate={(r) => save(r)} />
<RatingButtons layout="row" showInterval={false} />
```
