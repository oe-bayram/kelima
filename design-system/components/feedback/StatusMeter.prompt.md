Segmented bar showing how a chapter or the whole library splits across the 5 statuses (secure→new, left to right). Pairs with a legend of StatusBadge dots.

```jsx
<StatusMeter counts={{ secure: 10, known: 8, hard: 4, unknown: 2, new: 26 }} />
```
