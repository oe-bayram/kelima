Square icon-only control for TTS, favorite, close and nav. Three sizes, `active` for toggled state.

```jsx
<IconButton icon="volume-2" label="Aussprache anhören" variant="soft" />
<IconButton icon="star" label="Favorit" active />
```
